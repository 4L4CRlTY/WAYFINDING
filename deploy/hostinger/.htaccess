<IfModule mod_rewrite.c>
    RewriteEngine On

    # Forward Hostinger shared-hosting requests to Laravel's public folder.
    # The exclusion prevents public/public rewrite loops.
    RewriteRule ^$ public/index.php [L]
    RewriteRule ^((?!public/).*)$ public/$1 [L,NC]
</IfModule>
